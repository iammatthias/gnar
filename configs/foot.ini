# GNAR — foot terminal config.
# Larger default font + zero-padding so the dashboard's four panes are
# legible from across the room and use as much screen as possible.

[main]
font=monospace:size=14
pad=0x0
dpi-aware=auto

[colors]
alpha=1.0

[scrollback]
lines=10000
