# Drop-in for getty@tty1 that auto-logs in __USER__ so the Hyprland kiosk
# dashboard starts as soon as a display is attached. Other TTYs (tty2-6)
# still require a password.
[Service]
ExecStart=
ExecStart=-/usr/bin/agetty --autologin __USER__ --noclear %I $TERM
