[package]
name = "gnar-board"
version = "0.1.0"
edition = "2021"
description = "GNAR kiosk board — flicker-free ratatui rendering of container charts + stack status"
license = "MIT"

[dependencies]
ratatui = "0.29"
serde_json = "1"

[profile.release]
lto = true
strip = true
