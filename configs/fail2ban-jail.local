[DEFAULT]
# Arch + systemd writes auth events to the journal, not /var/log/auth.log.
backend = systemd
bantime = 3600
findtime = 600
maxretry = 3

[sshd]
enabled = true
port = ssh
maxretry = 3
bantime = 3600
