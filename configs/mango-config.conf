# GNAR — minimal mango config for an attached-display kiosk dashboard.
#
# When a display is plugged into the otherwise-headless server, getty@tty1
# auto-logs in, ~/.zprofile exec's mango, and this config runs the 4-pane
# `gnar-dashboard` tmux session fullscreen.
#
# Mango syntax: bind=MODIFIERS,KEY,DISPATCHER,ARGS
# Modifiers: SUPER, CTRL, ALT, SHIFT, NONE (combine with `+`).

# Auto-launch the dashboard. `--fullscreen` is a foot CLI flag that makes
# the terminal go fullscreen on startup; the tmux session inside fills it.
exec-once=foot --fullscreen -e gnar-dashboard

# Walk-up keybindings (only matter if you have a keyboard plugged in).
# Following Mango's default style: ALT for app actions, SUPER for compositor.
bind=ALT,Return,spawn,foot
bind=ALT,Q,killclient
bind=SUPER,F,togglefullscreen
bind=SUPER,M,quit
bind=SUPER+SHIFT,R,reload_config
